spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/lib?useSSL=false&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: hS4-MpW,8if6
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    open-in-view: true
  thymeleaf:
    cache: false
    mode: HTML5
    encoding: UTF-8
